import React from 'react';

const Header = () => (
  <div className="jumbotron jumbotron-fluid">
    <div className="container">
      <h1 className="display-4 align-top text-center">Memory Click</h1>
      <h3 className="lead text-center">Click on an image to earn points, but don't click on any more than once!</h3>
    </div>
  </div>
);

export default Header;